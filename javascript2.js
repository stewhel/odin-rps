function askNumber(){
    let playerNumber;

    playerNumber = prompt("Choose a number over 100")
    if ((playerNumber <= 100) || (playnumber == '')){
        askNumber()
    }else{
        return
    }
}